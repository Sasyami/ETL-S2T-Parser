# Краткая карта ETL S2T Parser

Этот файл — короткая точка входа для передачи проекта другой модели. Полное пользовательское описание, установка, схема данных и список инструментов находятся в `README.md`; обязательные правила разработки — в `AGENTS.md`. Не дублируйте их здесь.

## Назначение

Flask-приложение разбирает Excel-книги с Source-to-Target-маппингами, сохраняет исходные факты и результаты извлечения в SQLite, строит опциональную Neo4j-проекцию lineage и отвечает через инструментального LangGraph-агента.

## Основной поток

1. `processing/excel.py` разбирает листы и заголовки.
2. `storage/database.py` сохраняет структуру и исходные значения в SQLite.
3. `agents/sheet_group_classifier.py` выбирает группы листов.
4. Обработчики `sheet_skills/` записывают каталоги и S2T-строки.
5. `agents/summarizer_agent.py` одним LLM-вызовом строит описание файла.
6. `services/graph_sync.py` проецирует табличный и колонковый lineage в Neo4j.
7. `agents/agent.py` запускает read-only чатовый граф.

## Агентная часть

`agents/tools/routing.py` отдельным LLM-вызовом одновременно выбирает точные имена tools, runtime skills и схемы данных/маппинги. Результат формируется через `with_structured_output(ToolRoute, method="function_calling")` и проверяется по доступным каталогам; любой из трёх списков может быть пустым независимо от остальных. Эвристического fallback нет.

`agents/chat_graph.py` выполняет цикл planner → tool → observer → planner/responder. Следующий planner получает ограниченный последний tool-обмен и накопительную observer-выжимку; observer возвращает Pydantic-структуру `Observation`. Skills загружаются лениво и независимо от tools.

В изолированном worker цикл всегда проходит через ToolMessage. При `tools=[]` worker автоматически добавляет внутренний no-op `analyze_known_facts`: он не читает хранилища и только передаёт анализ известных фактов observer.

SQLite остаётся источником фактов. Neo4j содержит производную проекцию `ETLTable`/`TABLE_TRANSFORMS_TO` и `ETLColumn`/`TRANSFORMS_TO`; отсутствие данных в графе не доказывает их отсутствия в SQLite.

## Куда смотреть

- `README.md` — актуальное устройство, запуск, конфигурация, SQLite и HTTP API.
- `AGENTS.md` — ограничения и правила изменений.
- `agents/tools/registry.py` — фактический реестр инструментов.
- `config/` — группы листов и правила сопоставления колонок.
- `tests/` — проверяемое текущее поведение.
