# Доменные навыки получения данных read-only chat-agent

Skills задают только оркестрацию получения исходных данных: выбор tools,
аргументы, порядок чтения и проверку полноты. Атомарные контракты tools —
аргументы, ограничения, результат и ошибки — задаются только descriptions и
схемами tools. Производный анализ, сравнение и объяснение выполняет upstream
coordinator; worker должен лишь получить необходимые для них факты.

## S2T-строки

- Полный mapping между точными `source_table → target_table` читай одним
  directed exact reader активной палитры. Strict-reader читает всю table-пару
  без field-фильтров: если task задаёт поля, выбери их точным равенством из
  полученных строк upstream. Legacy-reader может принять одновременно оба поля.
  Не создавай отдельный вызов на каждую строку или поле; фрагмент имени ищи
  через `search_s2t_transformations`.
- Когда задана ровно одна таблица или `table.field` и её роль известна,
  используй exact reader этой фиксированной source/target-роли; точное поле
  выбери из результата. Если роль неизвестна, прочитай обе роли отдельными
  фиксированными readers. Не подменяй это направленной парой с придуманной
  второй таблицей.
- Явно названную таблицу передавай как exact arg strict-reader, а явно названное
  поле выбирай точным равенством из декодированных строк; field-args допустимы
  только у legacy-reader. Роль проверяй по фиксированной роли reader. Не
  склеивай таблицу и поле в needle подстрочного поиска.
- `source_table`, `source_field`, `target_table` и `target_field` — разные роли.
  Каждый ролевой фильтр должен быть задан task или подтверждён результатами;
  дополнительный фильтр является mismatch даже при 0 строк. Не зеркаль роли и
  сопоставляй запрошенные роли с фактически фильтруемыми и возвращаемыми полями.
  Факт из task повторно получать не требуется.
- Для покрытия таблицы одним полным вызовом получи все её S2T-строки reader-ом
  требуемой фиксированной роли и сравни множества upstream. Не перебирай поля
  последовательными вызовами.
- Списки и операции множеств source/target: `list_s2t_table_names`; он сам
  гарантирует корректную принадлежность имени ролям.
- `source_only` и `target_only` уже являются готовыми разностями множеств: не
  вычисляй противоположную роль вторым вызовом и не перепроверяй их через SQL.
- Для количества маппингов, полей и связанных таблиц по одной основной роли
  используй `summarize_s2t_tables`.
- Для произвольного среза или нестандартной агрегации используй `run_sql`.
- Для готового графа всех табличных связей используй
  `visualize_s2t_table_graph`, не вызывая перед ним невизуальные аналоги.
- s2t_transformations глобальна и никогда не ограничивается активным file_id.
- При сравнении каталога target-колонок с S2T сопоставляй `column_name` только с
  `target_field` и сохраняй фильтр `target_table`; для source-колонок используй
  только `source_field`/`source_table`. Не подменяй роли друг другом.
- «Трансформация» и «правило трансформации» означают `transformation_rule`.
- Для полной точной пары `source_table.source_field → target_table.target_field`
  сначала получи полный набор через directed exact reader с обеими таблицами,
  затем выбери строку с точным равенством обоих полей. Стрелка между точной
  парой сама по себе не означает путь.
- Если исходная task требует интерпретировать правило, получи фактический
  `transformation_rule` и точные source/target-роли. В данных нет стабильного
  group-id логической трансформации: не объединяй строки по сходству или
  равенству текста rule. Правило анализирует upstream coordinator.
- Одинаковые исходные строки являются отдельными фактами и не дедуплицируются.

- `trace_transformation_path` возвращает правила, additional objects, схему и
  подтверждение Neo4j. Для фрагмента имени сначала добавь
  `search_s2t_transformations`; не дублируй путь отдельными list/Neo4j tools.
- Downstream: source_table/source_field; upstream: target_table/target_field.
  Для lineage получи физические шаги
  `source_table.source_field → target_table.target_field` в исходном порядке.
- Если tool возвращает готовый `text_diagram`, сохрани его дословно как часть
  исходных данных для upstream.

## Neo4j

- Lineage колонки любой глубины: `trace_neo4j_lineage`; он объединяет точные
  связи и wildcard `* → *`.
- Wildcard — отдельный table-scoped `ETLColumn`; `COVERED_BY`/`EXPANDS_TO`
  связывают его с колонками. Шаг `* → *` переносит одноимённую запрошенную
  колонку: это перенос, не отсутствие маппинга. Рёбра показывай `source → target`.
- Прямой lineage таблицы: `trace_neo4j_table_lineage`; `table_exists` отличает
  отсутствующий узел от таблицы без подходящих рёбер.
- Путь между двумя таблицами: `trace_neo4j_table_path`; `chains` — точные
  цепочки, `paths` — полные рёбра.
- Полный направленный путь должен содержать оба конца и промежуточные узлы по
  порядку. Одного полного пути достаточно, если не запрошены все уникальные пути.
- Полный или транзитивный impact/downstream должен достигать конечных узлов;
  прямой переход до промежуточного узла недостаточен. Иначе явно сохраняй
  ограничение глубины.
- Если impact должен перечислить зависимые transformations, вызови
  `trace_neo4j_lineage` с `include_transformation_rules=true`: tool сам дочитает
  точные S2T-строки только по `transformation_id` из собственного lineage.
  Не передавай ID отдельному tool, не составляй свободный SQL и не ищи те же
  правила повторно по именам.
- Для произвольного графового обхода таблиц, нескольких условий, группировки
  или нестандартного среза используй `run_cypher`.
- Neo4j является синхронизированной проекцией: пустой графовый результат не
  доказывает отсутствие объекта или связи в SQLite.
- Колонковые узлы ETLColumn связаны TRANSFORMS_TO, табличные ETLTable —
  TABLE_TRANSFORMS_TO.

## Сравнение

- Для последующего сравнения нескольких объектов получи один и тот же
  необходимый набор фактов отдельно для каждого объекта.
- Порядок перечисления объектов не задаёт между ними source/target, upstream/
  downstream или иное направление. Не ищи связь или путь между объектами, если
  задача явно этого не требует.
- Сохраняй все явно заданные роли и фильтры каждого объекта. Не переноси фильтр,
  роль или найденный факт одного объекта на другой.
- Не формулируй общее и различия в worker. В кратком итоге перечисли, по каким
  объектам исходные данные получены и где их не хватает.

## Объяснение

- Для объяснения точной S2T-пары сначала вызови directed exact reader с обеими
  таблицами, затем выбери строки с точным равенством обоих полей и получи их
  фактический `transformation_rule`.
- Для последующего объяснения получи точные выражения, правила, роли и
  метаданные, явно запрошенные task. Не заменяй их пересказом worker.
- Не ищи дополнительные связи, которые не нужны для объяснения исходного
  объекта. Если исходных данных недостаточно или они противоречат друг другу,
  зафиксируй этот пробел в кратком итоге без производного объяснения.

## Excel и описания

- Для каталога загрузок используй `list_files`, а точное имя файла разрешай
  через `resolve_file` перед файловыми tools.
- Для листов и заголовков используй `list_sheets`,
  `list_file_sheet_headers` и `list_columns` по требуемому уровню детализации.
- Для поиска значения используй `search_excel_values`; если нужен контекст всей
  строки, следующим шагом вызови `get_excel_row` с фактическими идентификаторами
  из результата поиска.
- `search_excel_values` читает только сохранённые значения исходных Excel-ячеек
  из `data`. Не используй его для S2T, transformation_rule, SQL-признаков или
  физических строк логических ETL-таблиц.
- Семантика: `semantic_search_descriptions`; `scope`: `files`,
  `tables`, `source_tables`, `target_tables`, `columns`, `source_columns`,
  `target_columns` по домену, `all` — только без домена. Для колонок можно
  передать точные фильтры подвыборки до семантического ранжирования.
- Точные file/table/column читай exact catalog tool активной палитры: для
  source/target-пары предпочитай один pair-reader со всеми пятью обязательными
  идентификаторами. Если доступен batch metadata-reader, одну или несколько
  таблиц читай им с явным строковым `file_scope` (`"<file_id>"` из task либо
  `"all"`, когда его нет); иначе используй точные legacy-вызовы. Выбери нужную
  роль по `column_role`. Атрибуты и требуемое множество type/PK/not-null всегда
  отбирай по фактическим значениям полного результата, а не входным фильтром.
  Поиск фрагмента имени, типа или описания в подвыборке:
  `search_column_catalog`. Aliases физических заголовков исходных листов нужны
  только при загрузке и хранятся в `column_mapping.json`; в ETL-каталогах
  колонок aliases нет.
- Явное требование получить известные строки публичных
  `source_columns`/`target_columns` выполняй exact catalog tool; общий `run_sql`
  для последующего вычисления не заменяет этот факт получения.
- В каждый exact catalog call передавай все явно заданные идентификаторы этой
  операции. Нельзя опускать `file_id`/`table_name`, менять роль или расширять
  выборку. Если file_id не задан, используй только явный глобальный scope;
  цифры в имени таблицы не являются file_id. В legacy tool это означает точный
  `scope`; в role-neutral reader роль подтверждается `column_role`.
- Если source и target имеют разные table_name или column_name, получай их
  pair-reader либо отдельными точными ролевыми вызовами; не ищи target по имени
  source.
- Не выдумывай file_id, имена файлов, листов, колонок и таблиц.
